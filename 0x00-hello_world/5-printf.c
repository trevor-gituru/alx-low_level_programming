#include <stdio.h>
/**
 * main - Entry point of the program
 *
 * Description: This program prints a specific string using the puts function.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *message = "with proper grammar, but the outcome is a piece of art,\n";
	/* Using the prints function to print the string followed by a new line */
	printf("%s", message);
	/* End the program with the value 0 */
	return (0);
}
